[package]
name = "procmail-rs"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
regex = { version = "1.13.1", default-features = false, features = ["std", "perf-literal"] }
rustix = { version = "1.1.4", default-features = false, features = ["fs", "mm", "rand"] }
